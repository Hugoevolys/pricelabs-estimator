# URL du backend déployé sur Render.
# En local, laisse vide : Vite proxy /api vers http://localhost:3001 automatiquement.
# En prod (Netlify), mets l'URL Render, ex :
# VITE_API_BASE=https://pricelabs-estimator-backend.onrender.com
VITE_API_BASE=
